# The URL the site will be built for
base_url = "https://lucien-s.com"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = false

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

title = "Lucien Simpfendoerfer"
description = "Personal site"
generate_feed = true

taxonomies = [
    {name = "tags", rss = true},
    {name = "categories", rss = true},
]

[extra]
anpu_date_format="%e %B %Y"
anpu_menu_links = [
    { url = "$BASE_URL/photos", name = "Photos" },
    # { url = "https://www.linkedin.com/in/luciensimpfendoerfer/", name = "LinkedIn" },
    # { url = "mailto:lucien.simpfendoerfer@gmail.com", name = "Email" },
]
